<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.IProductNoticeViewDao">

	<select id="selectProductNoticeView" parameterType="int" resultType="product_notice_view">
		select * from product_notice_view where m_index=#{m_index}
	</select>
	
</mapper>