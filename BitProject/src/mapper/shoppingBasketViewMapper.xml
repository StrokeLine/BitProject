<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"     
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.IShoppingBasketViewDao">

	<select id="selectShoppingBasketView" parameterType="int" resultType="shopping_basket_view">
		select * from shopping_basket_view where m_index=#{m_index}
	</select>
	
	<select id="selectAllShoppingBasketView" parameterType="int" resultType="shopping_basket_view">
		select * from shopping_basket_view where m_index=#{m_index}
	</select>
	
</mapper>